This shall hold all the practical knowledge gained in the pursuit of this project:

* figured that if I return the entire html tag when it's is posted in html_mode via the bot,
    then the css as well as the urls will be rendered.
    so turns out telegram doesn't render the html into it's own images so you will need to replace them
* todo: in cooker module perhaps call get_sauce() inside cook_sauce()
* todo: in cooker module, when testing replace the get_sauce() with a local file
